<!DOCTYPE html>
<html>
  <head>
    <title>sample_EC-管理者</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <script src="https://kit.fontawesome.com/ab55497b72.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header>
      <div class="container">
        <div class="row navber navber-expand-lg navbar-dark bg-dark ">
          <div class="col">
            <div class="navber-brand display-3" style="color: white;">LOG</div>
          </div>
          <% if admin_signed_in? %>
            <div class="nav-item offset-1 col-2 p-4">
              <%= link_to 'ログアウト', destroy_admin_session_path, method: :delete, class: "btn btn-info" %>
            </div>
            <div class="nav-item col-2 p-4 text-center">
            <%= link_to 'ジャンル一覧', admin_genres_path, class: "btn btn-info" %>
          </div>
          <% end %>
        </div>
      </div>
    </header>
    <main>
      <div class="container">
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
        <%= yield %>
      </div>
    </main>
  </body>
</html>